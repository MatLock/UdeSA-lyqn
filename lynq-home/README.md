# LYNQ — Home

The landing page for **LYNQ**, where finding a job isn't a job of its own. A static site served from Cloudflare Workers.

## Deployment

```bash
npx wrangler login
npx wrangler deploy
```
